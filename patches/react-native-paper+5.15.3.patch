diff --git a/node_modules/react-native-paper/lib/commonjs/components/BottomNavigation/BottomNavigationBar.js b/node_modules/react-native-paper/lib/commonjs/components/BottomNavigation/BottomNavigationBar.js
index 07423bf..ed22132 100644
--- a/node_modules/react-native-paper/lib/commonjs/components/BottomNavigation/BottomNavigationBar.js
+++ b/node_modules/react-native-paper/lib/commonjs/components/BottomNavigation/BottomNavigationBar.js
@@ -397,10 +397,10 @@ const BottomNavigationBar = ({
     const v3InactiveOpacity = shifting ? inactiveOpacity : focused ? 0 : 1;
 
     // Scale horizontally the outline pill
-    const outlineScale = focused ? active.interpolate({
-      inputRange: [0, 1],
-      outputRange: [0.5, 1]
-    }) : 0;
+      const outlineScale = active.interpolate({
+          inputRange: [0, 1],
+          outputRange: focused ? [0.5, 0.9] : [0, 0],
+      });
     const badge = getBadge({
       route
     });
@@ -452,7 +452,7 @@ const BottomNavigationBar = ({
             translateY
           }]
         }]
-      }, isV3 && focused && /*#__PURE__*/React.createElement(_reactNative.Animated.View, {
+      }, isV3  && /*#__PURE__*/React.createElement(_reactNative.Animated.View, {
         style: [styles.outline, {
           transform: [{
             scaleX: outlineScale
diff --git a/node_modules/react-native-paper/lib/module/components/BottomNavigation/BottomNavigationBar.js b/node_modules/react-native-paper/lib/module/components/BottomNavigation/BottomNavigationBar.js
index 4500250..9806583 100644
--- a/node_modules/react-native-paper/lib/module/components/BottomNavigation/BottomNavigationBar.js
+++ b/node_modules/react-native-paper/lib/module/components/BottomNavigation/BottomNavigationBar.js
@@ -389,10 +389,11 @@ const BottomNavigationBar = ({
     const v3InactiveOpacity = shifting ? inactiveOpacity : focused ? 0 : 1;
 
     // Scale horizontally the outline pill
-    const outlineScale = focused ? active.interpolate({
-      inputRange: [0, 1],
-      outputRange: [0.5, 1]
-    }) : 0;
+      const outlineScale = active.interpolate({
+          inputRange: [0, 1],
+          outputRange: focused ? [0.5, 0.9] : [0, 0],
+      });
+
     const badge = getBadge({
       route
     });
@@ -444,7 +445,7 @@ const BottomNavigationBar = ({
             translateY
           }]
         }]
-      }, isV3 && focused && /*#__PURE__*/React.createElement(Animated.View, {
+      }, isV3  && /*#__PURE__*/React.createElement(Animated.View, {
         style: [styles.outline, {
           transform: [{
             scaleX: outlineScale
diff --git a/node_modules/react-native-paper/src/components/BottomNavigation/BottomNavigationBar.tsx b/node_modules/react-native-paper/src/components/BottomNavigation/BottomNavigationBar.tsx
index 17e4899..a59fcff 100644
--- a/node_modules/react-native-paper/src/components/BottomNavigation/BottomNavigationBar.tsx
+++ b/node_modules/react-native-paper/src/components/BottomNavigation/BottomNavigationBar.tsx
@@ -671,12 +671,10 @@ const BottomNavigationBar = <Route extends BaseRoute>({
               : 1;
 
             // Scale horizontally the outline pill
-            const outlineScale = focused
-              ? active.interpolate({
+              const outlineScale = active.interpolate({
                   inputRange: [0, 1],
-                  outputRange: [0.5, 1],
-                })
-              : 0;
+                  outputRange: focused ? [0.5, 0.9] : [0, 0],
+              });
 
             const badge = getBadge({ route });
 
@@ -740,7 +738,7 @@ const BottomNavigationBar = <Route extends BaseRoute>({
                       },
                     ]}
                   >
-                    {isV3 && focused && (
+                    {isV3 && (
                       <Animated.View
                         style={[
                           styles.outline,
